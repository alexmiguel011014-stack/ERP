// Servido via app://renderer/... (ver main.js) — a origem é a raiz, não um
// subcaminho, então nenhum prefixo é necessário (nem em dev nem em produção).
export const getImagePath = (path: string): string => path;
